version: '3.8'

services:
  backend:
    build: ./RAG
    ports:
      - "8000:8000"
    volumes:
      - ./RAG:/app  # 热更新
    environment:
      - ENV=production

  frontend:
    build: ./web
    ports:
      - "5173:5173"
    volumes:
      - ./web:/app
    depends_on:
      - backend